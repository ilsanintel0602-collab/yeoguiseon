# HANDOFF 2026-05-21 v8 — v5.10 push 완료 + v5.11~v5.15 push 대기

> 2026-05-21 후반 작업 정리. 다음 세션 시작 시 이 문서 먼저 읽기.

---

## 🚦 다음 세션 첫 행동 (1줄)

```
E:\Cowork 작업\yeoguiseon-v4\scripts\auto_push.bat   ← 더블클릭
```

→ v5.11~v5.15 + 아이콘 6개 한 묶음 push.

---

## 오늘 push 완료 (시간순)

### Push #1 — commit `fa3e64a37790b8bc9864013961e1bd3adddced6e`
**v5.10 (라이브 박스 정직화 + 압력솥/프라이팬 명시 + sourceName 일괄)** push 완료.
모바일에서 v5.10 → v5.12 확인됨 (사용자 검증).

---

## ⏳ Push 대기 (외장하드 변경 → 다음 push 한 번에)

### v5.11 — Gemini JSON 복구 + detect threshold 강화
- `analyzeWithLLM` 자동 복구 로직 (Unterminated string 등)
- detect threshold 0.3 → 0.5

### v5.12 — 미활용 데이터 통합
- `official_classification` 결과 카드 노출 (🏷️ 환경부 분류 트리)
- `regions_meta.phone` 표시 (공식 안내 옆 031-...)

### v5.13 — 카테고리별 봉투 + 아이콘 cache-bust
- 음식물 카테고리 → "🍱 음식물 봉투 가격"
- 일반/위험물/의류/의약품 → "🛍️ 종량제봉투"
- 재활용 → 가격 표시 안 함 (의도적)
- `manifest.json` icons에 `?v=12` query (아이콘 cache-bust)
- 새 아이콘 6개 PNG 생성 완료 (`icons/` 폴더)

### v5.14 — 디자인 옵션 B 적용 (가장 큰 UX 도약!)
- **라이브 박스 제거** (COCO-SSD detect 호출 → 0)
- **우아한 촬영 가이드** (4 모서리 펄스 brackets + 중앙 십자선)
- "병 67%", "chair" 같은 잘못된 첫인상 라벨 완전 차단
- 모바일 배터리 큰 절약

### v5.15 — Phase D 위험물 UI + 정성 디테일
- 위험물 카드 펄스 애니메이션 (빨간 배경 + 큰 아이콘 + 펄스)
- 종류별 안내 (가스/리튬배터리/주사기·약/날카로운 유리)
- 촬영 시 햅틱 진동 (`navigator.vibrate(20)`)
- 결과 카드 하단 👍/👎 피드백 버튼 (Phase 8 seed, localStorage 누적)
- "포스터" → 합성수지 필름 alias 추가 (환경부 similar 미매칭 1개)

---

## 데이터 자산 100% 활용 상태

| 자산 영역 | 환경부 매칭 | 활용도 |
|---|---|---|
| `national_rules.json` 738+ items | 환경부 692 unique 100% 매칭 | sourceUrl·feature·caution·classification 모두 노출 |
| 환경부 `similar` 12,798개 | 99.98% 우리 aliases에 있음 | 1개만 미매칭 (해결됨) |
| `regions_meta.json` 250 시군구 | officialUrl + phone 모두 활용 | ✅ |
| `bag_prices.json` 249 시군구 | 가정용 + 음식물용 분기 매칭 | ✅ |
| `region_urls.json` 32 시범 | bulkWasteUrl 가구·전자 카드 | ✅ |

→ **데이터 활용도 ~98%** (남은 2%는 의도적 백업·미래용)

---

## 변경 파일 (오늘 누적, push 대기)

```
[코드]
  app.html               v5.10 → v5.15 (Gemini JSON 복구 + 분류 트리 + 카테고리별 봉투 + 라이브 박스 제거 + 위험물 UI + 햅틱 + 피드백 버튼)
  sw.js                  VERSION v5.10 → v5.15
  manifest.json          icons `?v=12` cache-bust

[아이콘]
  icons/icon.svg         새 디자인
  icons/icon-192.png     새 PNG (PIL 생성)
  icons/icon-512.png     새 PNG
  icons/apple-touch-icon.png   새 PNG
  icons/favicon-32.png   새 PNG
  icons/icon-192-maskable.png  새 PNG
  icons/icon-512-maskable.png  새 PNG

[데이터]
  data/national_rules.json     "포스터" alias 1개 추가 (합성수지 필름)
  data/region_urls.json        (그대로)
  data/national_rules.json.backup_pre_v5_13_polish.json (새 백업)

[스크립트]
  scripts/generate_icons.py    (새, PIL 아이콘 생성기)
  scripts/generate_icons.bat   (사용자 더블클릭용)
  scripts/refine_v5_10.py      (이미 있음)
  scripts/release_v5_10.bat    (이미 있음)

[문서]
  docs/DATA_INVENTORY.md       v5.13 갱신 (활용 매트릭스)
  docs/PHASE4_PLAN.md          (그대로)
  HANDOFF_2026-05-21_v8.md     이 파일
```

---

## 다음 세션 진행 순서

### Step 1 (5초) — Push
`scripts/auto_push.bat` 더블클릭 → 엔터 → Done.

### Step 2 (1분) — 모바일 확인
1. 새로고침 1~2번 → 상단 **v5.15** 표시
2. PWA 재설치 (홈 화면 아이콘 길게 → 제거 → 사이트 재방문 → "홈 화면에 추가")
   → manifest `?v=12` 덕분에 **새 아이콘** 받음
3. 카메라 켜기 → **라이브 박스 사라지고 4 모서리 펄스 가이드만** 확인

### Step 3 — 사물 있을 때 검증
- 압력솥 → 일관성 (4번 시도 → 1번 성공 기대, JSON 복구 효과)
- 후라이팬 → 정확 분류
- 가스통/리튬배터리 (위험물) → 빨간 펄스 강조 카드
- 음식물 (사과 껍질 등) → 음식물 봉투 가격 표시
- 페트병 → 가격 표시 안 함 (재활용은 봉투 불필요)

---

## Phase 진행 상태

| Phase | 상태 | 점수 |
|---|---|---|
| 1. 코어 데이터 | ✅ 종결 | 100/100 |
| 2. 지역 확장 | ✅ NEAR 종결 | 90/100 |
| 3. 데이터-앱 연동 정비 | ✅ 종결 | ~97/100 |
| 4. 추가 데이터 보강 | PLAN 작성 | 보류 (publicDataPk 검색 필요) |
| 5. 벤치마크 자동화 | 대기 | — |
| 6. UX 차별점 | **🆕 일부 적용** (v5.14 디자인 + v5.15 위험물 UI + 햅틱) | 진행 중 |
| 7. 배포 통합 push | ⏳ 다음 세션 첫 작업 | — |
| 8. 사용자 피드백 시스템 | **🆕 seed 시작** (v5.15 👍/👎 버튼) | 데이터 누적 시작 |

---

## 핵심 원칙 (다음 세션도 절대 지킴)

1. **본질**: 정확하고 신속한 분리안내 + RecycleAI 동등/우위
2. **95+ 자동 다음 단계**
3. **반복 vs 오류 명확** (헛수고 X, 오류 시정 ✓)
4. **VERSION 4곳 동기** (title/brand/sw.js)
5. **백업 자동** (`*.backup_pre_*.json`)
6. **data-steward SKILL = 데이터 작업 헌법**
7. **사용자 더블클릭 최소화**

---

## 다음 세션 시작 권장 메시지

> "HANDOFF_v8 읽고 push부터 진행해줘"

이 한 마디로 다음 세션이 자연스럽게 이어집니다 🌿
