# 여기선 v4 — GitHub에 올리지 않을 파일/폴더

# 데이터 정리 시 자동 생성되는 백업 (로컬 안전망)
data/*.backup_*.json
data/national_rules.backup_*.json
data/*.bak
data/*.bak.json

# 작업 보조 폴더 (인수인계용)
_바탕화면_복사용/

# OS·에디터 임시 파일
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# VS Code / IntelliJ 사용자 설정
.vscode/
.idea/

# Node 의존성 (혹시 나중에)
node_modules/

# 로컬 환경변수 (혹시 나중에)
.env
.env.local

# Personal Access Token (GitHub 자동 push용, 절대 GitHub에 노출 X)
.git_pat.txt
.git_pat
git_pat.txt
*_pat.txt
