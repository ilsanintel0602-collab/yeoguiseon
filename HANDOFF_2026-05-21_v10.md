# HANDOFF 2026-05-21 v10 — 카톡 공유 시점

> 2026-05-21 v5.21 + Worker v1.5 + Phase 8 자동 시스템 + 카톡 미리보기 정리 완료.
> 다음 세션 시작 시 이 문서만 읽으면 됨.

## 🚦 현재 상태 (한눈에)

| 항목 | 값 |
|---|---|
| 모바일 적용 버전 | v5.21 (commit `9197fb150...`) |
| GitHub Pages | https://ilsanintel0602-collab.github.io/yeoguiseon/app.html |
| 카톡 미리보기 | ✅ 깔끔 (디버그 X) |
| Worker | v1.4 활성 (KV 미설정), Cloudflare 코드에 v1.5 있지만 재배포 안 함 |
| 데이터 | 765 items, 정제된 aliases ~2061, sourceUrl 96.9% |
| 자체 audit | 100/100 |
| 일상어 매칭 | 100% (101/101) |

## 오늘 누적 작업 (v5.10 → v5.21 + Worker v1.5)

### 큰 도약
- **v5.14**: 라이브 박스 → 우아한 가이드 (옵션 B)
- **v5.20**: 라이브 박스 부분 복원 (threshold 0.7+ 만)
- **v5.21**: 속도 큰 업그레이드 (이미지 50%↓ + detect 1500ms)

### 데이터
- 환경부 730 100% 통합
- 일상어 30% → 100% 매칭 (337+ 단어 보강)
- 카테고리 17/17 균형
- 중복 alias 11,012건 정리
- AA OCR 버그 해결
- 1.8MB → 1.3MB 슬림화 (28% ↓)

### 시스템
- data-steward 스킬 (헌법)
- phase-progression 스킬
- PHASE_CRITERIA / CONSISTENCY_RULES / DATA_INVENTORY / WORK_HISTORY
- data_audit_full.py (10 stage 자체 점검)
- benchmark.py (Phase 5 PC 인프라)
- **analyze_feedback.py (Phase 8 자동 보강 사이클)**
- Worker v1.5 `/feedback` 엔드포인트
- 카톡 OG 메타 태그 (미리보기 정리)

## 카톡 공유 메시지

```
🌱 여기선 - 분리수거 AI 도우미
사진 찍으면 우리 동네 분리수거 방법 알려줘요.

🔗 https://ilsanintel0602-collab.github.io/yeoguiseon/app.html

📲 Chrome 또는 삼성 브라우저 → 홈 화면에 추가
```

## 🎯 다음 세션 우선순위

### A. 박스 탭 자동 분석 복원 (사용자 명시 요청)
이전 (v5.13 이전) 기능: 라이브 박스 탭 → 자동 분석.
```js
// drawBoxes에 클릭 핸들러 추가
// 탭한 박스의 bbox cropped → handleCapture 자동 호출
```

### B. 피드백 누적 시 자동 보강
```
1) 사용자 모바일에서 피드백 누적
2) Cloudflare KV (Worker v1.5 재배포 시) 또는 localStorage
3) python scripts/analyze_feedback.py --dump fb_dump.json
4) 자동으로 약점 식별 + 카테고리 분류 + alias 추가
5) audit + push
```

### C. Cloudflare Worker v1.5 재배포 (선택, 자동 시스템 활성용)
- Cloudflare dash → yeoguiseon-proxy → Edit code
- 새 cloudflare_worker.js 코드 붙여넣기
- (선택) Settings → KV Namespace Bindings → "FEEDBACK_KV" 생성
- Save and Deploy

→ 활성 후 모바일 사용자 👍/👎 → KV 자동 누적.

### D. (장기) 진짜 자동 데이터 파이프라인
- data.go.kr 자동 publicDataPk 검색
- 시군구 250 홈페이지 자동 크롤링
- 매주 GitHub Actions 자동 실행 (또는 사용자 PC cron)

## 다음 세션 시작 권장 메시지

```
"카톡 피드백 받은 거 알려줘요. 그리고 박스 탭 자동 분석 복원부터"
```

또는:
```
"analyze_feedback.py 실행해서 자동 보강 사이클 가동"
```

## 핵심 원칙 (절대 변경 X)

1. 본질: 정확하고 신속한 분리안내 + RecycleAI 동등/우위
2. 95+ 자동 다음 단계
3. 반복 vs 오류 명확
4. VERSION 4곳 동기
5. **수동 alias X — 자동 시스템 O**
6. data-steward SKILL = 헌법

## 파일 위치

```
E:\Cowork 작업\yeoguiseon-v4\
├── app.html           v5.21 + OG 메타
├── sw.js              VERSION v5.21
├── scripts/
│   ├── auto_push.bat            ← 다음 push 시 더블클릭
│   ├── analyze_feedback.py      ← 자동 보강 사이클
│   ├── cloudflare_worker.js     v1.5 (재배포 대기)
│   ├── data_audit_full.py
│   ├── benchmark.py
│   └── ... (그 외 누적)
├── data/
│   ├── national_rules.json      765 items, 1.3MB
│   ├── ocr_keywords.json        (AA 위험 제거됨)
│   ├── region_urls.json         32 시범 지역
│   ├── regions_meta.json        250 시군구
│   └── *.backup_pre_*.json      모든 백업
├── docs/
│   ├── PHASE_CRITERIA.md
│   ├── CONSISTENCY_RULES.md
│   ├── DATA_INVENTORY.md
│   ├── WORK_HISTORY.md
│   ├── PHASE3_DESIGN.md
│   ├── PHASE4_PLAN.md
│   └── HANDOFF_2026-05-21_v10.md  ← 이 파일
└── skills/
    ├── data-steward/SKILL.md
    └── phase-progression/SKILL.md
```
