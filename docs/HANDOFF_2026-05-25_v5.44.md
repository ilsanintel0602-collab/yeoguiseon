# HANDOFF 2026-05-25 — v5.44 (시연 미실시)

## 한 줄 요약
v5.36 → v5.43 → v5.44 (모호 UI 활성화). Worker v1.9.7 → v1.9.13 (Post-AI 가드레일). 정확도 88.6/100. **시연 미실시 — 다음 세션 시작 시 시연 결과 받아 정정**.

## 라이브 상태
- 앱: **v5.44** (사용자 push 액션 대기, v5.43 push 완료)
- Worker: v1.9.12 라이브, **v1.9.13 deploy 대기** (Post-AI 가드레일)
- D1: 761/8516 (drift, 라이브 미사용)
- 정적 JSON: **773 items / 7972 aliases**

## 이번 세션 누적 (v5.36 → v5.44)

### 데이터 자산
| 지표 | 시작 | 끝 |
|---|---|---|
| NATIONAL 출처 보유 | 88% | **100%** (정확 91%) |
| alias 충돌 | 722 | **30** (-96%) |
| cityGuide 평균 키 | 4.1 | **9.9** |
| 카테고리 표준 | 17 임의 | **한국 분리배출.kr 12+8** |
| region_urls 시·구 | 35 | **239** (wasteUrl 233 - 14 죽은 = 219) |
| 정량 점수 | 측정 X | **88.6/100** |

### 시스템 정착
- **quick_check 4중 검사** (truncation + IIFE + init + JS syntax) + DB 벤치 점수 임계값 80
- **auto_snapshot** (대책 C) — truncation 11번 자동 회복
- **benchmark_db.py** — 정량 평가 (분기 자동화)
- **check_urls_alive.py** + GitHub Actions 월간 cron
- **learn_from_feedback.py** — 사용자 피드백 학습
- **auto_push.ps1** — quick_check + snapshot 자동 통합 (v5.44)
- **ambiguous_map.json** — 재질 모호 2건 (라이스컨테이너·포장지) — v5.44 UI 활성

## v5.44 변경

### 코드
- `app.html` 결과 카드에 **rule.ambiguous 표시 UI** 추가 (노란 박스, "같은 이름 다른 재질·용도")
- `app.html` 버전 v5.43 → v5.44
- `sw.js` VERSION = 'v5.44'

### 데이터
- `data/ambiguous_map.json` — 자동 fetch (matchRule에서 _ambig 조회)
- `data/region_urls.json` — 죽은 URL 7건 제거 (v5.43.1)
- `data/region_exceptions.json` — cityGuide 죽은 URL 7건 제거

### 문서
- `docs/FEEDBACK_LEARNING_GUIDE.md` — 학습 루프 매뉴얼
- `docs/HYBRID_MATCHING_DESIGN.md` — v6.0 카메라 로드맵
- `docs/HANDOFF_2026-05-25_v5.44.md` — 이 문서

## 사용자 액션 대기 (push 후)
1. v5.44 push: `scripts\auto_push.bat` (자동 quick_check + snapshot)
2. Worker v1.9.13 deploy: `npx wrangler deploy`
3. 시연 검증 (사용자 외출 후)
4. (베타) FEEDBACK_DUMP_KEY 등록

## 다음 세션 시작 가이드
1. 메모리 `project_v4_status.md` 읽기
2. 시연 결과 받기 (사용자 캡처 + 사고 보고)
3. 정정 작업 + 추가 단계

## 다음 우선순위 (사용자 결정 대기)
1. **MediaPipe Object Detection** ⭐ — COCO-SSD 대체. on-device ML Kit 수준 정확도 (4-6시간)
2. **사진 벤치마크 가동** — 사용자 사진 10~30장 → pass@1 측정
3. 시·구청 본문 정독 — 큰 도시 cityGuide 풍부화
4. **app.html 모듈 분할** — truncation 영구 해결 (1-2일)
5. D1 라이브 전환
6. 자체 한국 분리수거 모델 (Roboflow)

## 핵심 함정 (반복 사고 방지)
- **Edit 도구 한국어 큰 블록 = truncation** (Python fsync 회피 룰. 11번 사고)
- COCO-SSD 한국 사물 false-positive (살충제→bottle. v5.42 박스 폐기)
- 시·구청 SSL 인증서·핸드쉐이크 = Python urllib 실패 (실제 Chrome OK)
- 분리배출.kr 인덱스 URL 일부 죽음 (14건 정리, timeout 22개 보류)

## 본질 가치 자기 평가 (객관 기준)
| 영역 | 여기선 | 업계 양호 수준 |
|---|---|---|
| 데이터 출처 정직성 | ✅ A/B/unknown 라벨 | 일반적으로 미공개 |
| 카메라 정확도 | ❌ COCO-SSD 한계 | ✅ on-device ML Kit + 다중 인식 |
| UX 정보 구조화 | △ 단계 1·2 보강 | ✅ 카드형·핀치 줌 |
| 정량 측정 | 88.6/100 (DB) | pass@1 약 95% (사진) |
| 비용·확장 | Cloudflare 무료 가능 | 유료 백엔드 (₩60K/월 수준) |

## 시연 후 받은 사고들 (정정 완료)
- v5.39 → v5.40: 압력솥 종량제 잘못 → matchRule alias 정정
- v5.40 → v5.41: 머그·살충제·대형폐기물 URL 정정
- v5.41 → v5.42: GPS·COCO 박스·외국인 안내·중복·전화 라벨 (7가지)
- v5.42 → v5.43: 카테고리 표준 + 시·구 cityGuide 보강
- v5.43.1: 죽은 URL 14건 제거
- v5.43 → v5.44: 모호 분기 UI 활성

= 9개 시연 사이클 정정. 사용자 본질 원칙 ("벌금 10만원") 강하게 적용.
