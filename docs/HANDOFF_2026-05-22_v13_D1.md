# HANDOFF 2026-05-22 v13 — D1 인프라 완성 + v5.28 점진적 전환 + 모바일 회귀 발견

## 한 줄 요약

Cloudflare D1 인프라 100% 구축 (DB·KV·Worker·wrangler.toml 다 살아있음). 앱은 v5.28.1까지 작성 완료. 모바일에서 4가지 회귀 발견 → 핫픽스 일부 적용, 나머지는 다음 세션에서 진단 필요.

---

## 오늘 완료 (인프라)

### Phase A1 — D1 마이그레이션 ✅
- **D1 DB**: `yeoguiseon-db` (UUID `743d3dc0-2c01-4780-a79a-21d8b1aae91b`, APAC, 2.27MB)
- **데이터**: 765 items + 8,529 aliases + 250 regions + 45 region_exceptions
- **KV**: `RATE_LIMIT_KV` (id `0136af9b67b7422b90f17aa581afbf30`) — rate limit + 24h 이미지 캐싱
- **wrangler.toml**: 프로젝트 루트, D1 + KV 바인딩 완료. 이제 `wrangler deploy` 한 줄로 배포
- **Worker v1.9.2**: env.DB + env.RATE_LIMIT_KV 바인딩, /data/* GET 엔드포인트 + /data/all (정적 호환 포맷)

### 사용자 액션 한 번 (15분) — 완료
1. ✅ Node.js → npm install -g wrangler
2. ✅ wrangler login (브라우저 OAuth)
3. ✅ wrangler d1 create yeoguiseon-db
4. ✅ wrangler kv namespace create RATE_LIMIT_KV
5. ✅ d1_schema.sql + v7_initial.sql 적용 (DROP → CREATE → INSERT)
6. ✅ wrangler deploy (env.DB·KV 바인딩 활성화)
7. ✅ /data/all curl 테스트 — 765 items 응답 확인

### v5.28 push 완료 (commit a18066e)
- app.html v5.28: D1 우선 + 정적 폴백
- Worker v1.9.2: /data/all 신규
- d1_schema.sql FK 제거 (D1은 self-ref/forward-ref FK 거부)
- d1_migrate.py 자동 필터 (FK 위반 item_id 자동 제외)
- data/migrations/v7_initial.sql (BEGIN/COMMIT 제거 + FK 위반 8건 제거 후 9,597 INSERT 정상)
- scripts/d1_reset.sql 신규 (비상시 사용)
- region_exceptions.json: 41430 의왕시 정정, 48820 라벨 정리, 끝 null 바이트 14개 제거
- quick_check.py: v5.28 검사 + 통합시 화이트리스트 (41130 성남·43110 청주·44130 천안·45110 전주·47110 포항·48120 창원)
- auto_push.ps1 패턴 확장 (.sql, .toml, .template, data/migrations/, .github/workflows/)

---

## 오늘 발견한 모바일 회귀 (v5.28 push 후)

사용자가 v5.28 push 후 모바일 PWA에서 보고한 5개 이슈:

### P0 핫픽스 v5.28.1 (작성 완료 — push 여부 확인 필요)

1. **갤러리 버튼이 카메라를 띄움** → `<input ... capture="environment">`에서 `capture` 제거 ✅ 수정
2. **D1 응답이 정적 JSON 메타데이터(categories 등) 누락** → 안전 병합 모드 ✅ 수정
   - 정적 JSON 우선 로드 → D1 성공 시 items만 덮어쓰기 → D1 실패 시 정적 그대로
3. **Gemini REQUEST_REJECTED 메시지 친절화** ✅ 수정
   - "🛡️ 이미지 안전성 검사에 걸렸어요. 사람·얼굴·민감 정보 포함 가능. 사물만 다시 찍어주세요."
4. **분석 실패 진단 강화** ✅ 수정
   - renderResult 실패 vs analyzeHybrid 실패 구분
   - console.error에 err 객체 + result 함께 출력

### 다음 세션에서 해결 (v5.28.2)

5. **"이름으로 찾기" 모달의 input에 글자가 안 써짐** ⚠️ 미해결
   - 코드상 핸들러 정상 (line 2724 `$('searchInput').oninput = ...`)
   - 모바일 키보드 안 뜸 / 모달 z-index / 다른 요소가 위에 있음 의심
   - 진단 필요: 모바일 Chrome 개발자도구로 모달 요소 inspect

6. **카메라 인식 박스 "지저분"** ⚠️ 미해결
   - 어떤 부분이 지저분한지 사용자가 구체적으로 말해야 진단 가능
   - 가능성: 라이브 디텍션 박스 깜빡임 / 가이드 라인 산만 / 모서리 노이즈

7. **"AI 분석" 상태 표시 사라짐** ⚠️ 미해결
   - v5.27 이전엔 'AI 분석' 메시지 또는 배지가 있었음
   - 현재 renderLoading('🤖 AI가 인식 중...') 호출되지만 사용자가 못 봤다 함
   - sheet가 안 열리거나, 너무 빨리 결과로 넘어가 보일 시간 부족

---

## 현재 시스템 상태

### 라이브 서비스
- **PWA**: https://ilsanintel0602-collab.github.io/yeoguiseon/ (v5.28 또는 v5.28.1)
- **Worker**: https://yeoguiseon-proxy.ilsanintel0602.workers.dev (v1.9.2)
- **D1**: yeoguiseon-db, 2.27MB, 9,590 rows
- **KV**: RATE_LIMIT_KV (rate limit 분당 60 + 일당 2000 + 이미지 24h 캐싱)

### Worker 엔드포인트
- POST `/` — Gemini 이미지 분석 + Claude 폴백 (앙상블)
- POST `/augment` — Gemini alias 증강 (서버 스크립트용)
- POST `/feedback` — 사용자 피드백 수집 (KV/D1)
- GET `/data/all` — 전체 items + aliases (정적 호환 포맷, 1.38MB, 1h 캐시) 
- GET `/data/items` · `/data/items/:id` · `/data/search?q=...`
- GET `/data/regions` · `/data/regions/:code` · `/data/region/:code/exceptions`

### 데이터 파일 (정적 JSON, 폴백 보장)
- data/national_rules.json (765 items + 카테고리 메타)
- data/regions_meta.json (250 시군구 + 좌표)
- data/region_exceptions.json (228 시군구 cityGuide + 45 exception 룰)
- data/regions_meta + region_exceptions + ocr_keywords + brand_db + bag_prices + recycle_centers + region_urls

---

## 다음 세션 진입 순서 (우선순위)

### 즉시 (v5.28.1 push 안 했다면)
```
E:\Cowork 작업\yeoguiseon-v4\scripts\auto_push.bat 더블클릭
```
→ Y → 1~2분 → GitHub Pages 자동 반영

### v5.28.2 핫픽스 진단 (모바일 4가지)
1. **검색 input 안 써짐** — 모바일 Chrome DevTools 원격 디버깅 (USB 연결)으로 모달 요소 확인
2. **카메라 박스 지저분** — 사용자에게 구체적으로 묘사 요청 또는 캡처
3. **"AI 분석" 표시 사라짐** — sheet 표시 timing + 메시지 확인
4. **분석 실패 케이스 추가 진단** — console.error 로그로 정확한 원인 파악

### Phase A2 (옵션, 인프라 활용)
- `.github/workflows/crawl_weekly.yml` GitHub 웹 UI에서 수동 업로드 (PAT workflow 권한 없어서 자동 push 불가)
- DATA_GO_KR_API_KEY를 Cloudflare Worker Secrets에 등록
- 매주 일요일 06:00 KST 자동 크롤링 → D1 갱신

### Phase B/C/D (큰 그림)
- B2: 자체 CNN 모델 (TensorFlow.js)
- B3: 한국어 벡터 검색 (semantic matching)
- B4: 사용자 피드백 → 자동 학습 사이클
- C1: GPS + 가까운 수거함 지도
- C2: 음성 입력 (Web Speech API)
- D1: Sentry + Cloudflare Analytics
- D2: 벤치마크 자동 매주 측정

---

## 인프라 자산 (분실 시 재생성 비용)

| 자산 | 값 | 재생성 비용 |
|---|---|---|
| D1 database_id | 743d3dc0-2c01-4780-a79a-21d8b1aae91b | wrangler.toml에 보존됨 |
| KV namespace id | 0136af9b67b7422b90f17aa581afbf30 | wrangler.toml에 보존됨 |
| Worker URL | yeoguiseon-proxy.ilsanintel0602.workers.dev | 고정 |
| GEMINI_API_KEY | Cloudflare Worker Secret (대시보드) | 별도 보관 |
| GitHub PAT | E:\Cowork 작업\yeoguiseon-v4\.git_pat.txt (로컬 only) | github.com/settings/tokens 재생성 |

**모든 자산이 외장하드 E:\에 있어요. wrangler.toml만 있으면 다 복구 가능.**

---

## 다음 세션 첫 인사용

> 안녕하세요 경숙님! 어제(2026-05-22) D1 인프라 100% 셋업 끝났고 v5.28.1 핫픽스까지 작성했어요.  
> 모바일에서 4가지 회귀 발견됐는데 다음 세션에서 진단하기로 했어요:  
> ① 갤러리 카메라 → 핫픽스 적용 ② 검색 input 안 써짐 ③ 카메라 박스 지저분 ④ AI 분석 표시 사라짐.  
> 모바일에서 v5.28.1 다시 체험하셨는데 어떤 게 고쳐졌고 뭐가 남았나요?

---

## 안 잊을 것

- D1은 self-referential FOREIGN KEY · forward-ref FOREIGN KEY 거부 → schema에 FK 쓰지 말 것
- D1 SQL 파일에 BEGIN TRANSACTION / COMMIT 쓰지 말 것
- PAT가 fine-grained면 `.github/workflows/` 수정 시 "Workflows" 권한 필요 (없으면 404)
- 자체 점검 룰: push 전 quick_check.py 끝까지 ✅ — 한 번 ❌도 추적해서 잡기
- 모바일 v5.28 회귀: D1 응답을 그대로 NATIONAL로 쓰면 categories 등 메타데이터 누락 → 정적 우선 + items만 병합으로 해결
