# HANDOFF v11 — 2026-05-21

## 📊 오늘 큰 진척 한눈에

| 영역 | 변화 | 효과 |
|---|---|---|
| **앱 버전** | v5.5/v5.22 → **v5.24** | 분리수거에 적합한 Worker 자동 호출, _inherits 상속, cityGuide UI |
| **Worker 인프라** | v1.4 → **v1.7.2** | `/augment` 엔드포인트 + 유연한 파서 + maxTokens 2048 |
| **데이터: alias** | 2,061 → **2,336** (+275) | 룰 기반 사전 증강 (Gemini 호출 0) |
| **데이터: 일산동구 룰** | 30 → **37** (+7) | 휴지·물티슈·기저귀·캡슐·빨대·젓가락 등 일상 품목 |
| **데이터: 시군구 커버** | 4개 → **11개** | +파주·김포·의정부·부천·마포·은평·서대문 |
| **인프라** | augment_aliases.py v2 | 25개마다 자동 저장, 끊김 안전, 이어가기 지원 |

## 🔧 v5.24 핵심 변경 (앱 코드)

### 1. `state.apiKey` 기본값 `'WORKER'`
- 본인 키 안 넣어도 자동으로 Cloudflare Worker 사용
- COCO-SSD 폴백 제거 (분리수거에 맞지 않음 — 사용자 지적)

### 2. `_inherits` 체인 처리 (matchRule 함수)
- 덕양구(41281)·일산서구(41287)가 일산동구(41285) 룰 자동 상속
- 같은 데이터 자산으로 **3배 시군구 커버리지**
- 무한 루프 방지 safetyCounter

### 3. cityGuide UI 통합 (renderResult)
- 카테고리별 핵심 정보 자동 표시:
  - 전자: 폐가전 무상수거 전화 (1599-0903)
  - 가구: 대형폐기물 신고 URL
  - 종이/종이팩: 종이팩-화장지 교환 사업
  - 음식물: 배출 시간
  - 일반/건전지/형광등: 봉투 색 + 구매 링크
- 공통: 시청 자원순환과 / 구청 환경녹지과 연락처
- 결과 카드 정보 풍부도 대폭 향상

## 🏗 Worker v1.7.2 핵심

### 새 엔드포인트
- `/feedback` (v1.5) — 사용자 피드백 자동 수집
- `/augment` (v1.6) — 텍스트 alias 데이터 증강

### 보안
- `/augment`만 Origin 검증 우회 (Python 서버간 호출용)
- IP rate limit: 분당 60회 / 일당 2000회 (무차별 호출 방지)
- Gemini API 키는 Cloudflare 환경변수에만 존재 (사용자 평생 안 봄)

### 안정성
- 유연한 파서: `[...]` 직접 배열 / `{aliases:[...]}` 객체 / 코드펜스 다 잡음
- maxOutputTokens 2048 (한국어 alias 응답 잘림 해결)
- 디버그 필드: `parseHow`, `raw_preview`

## 🛠 새 스크립트

| 파일 | 용도 |
|---|---|
| `scripts/augment_aliases.py` | v1 — Worker /augment 호출 (마지막에 한 번 저장) |
| `scripts/augment_aliases_v2.py` | **v2 — 25개마다 자동 저장, 이어가기 가능** ⭐ 권장 |
| `scripts/analyze_augment.py` | augment 결과 비교 (카테고리별 증가량) |
| `scripts/expand_aliases_rules.py` | 룰 기반 사전 증강 (Gemini 0) |
| `scripts/test_worker.py` | Worker /augment 진단 도구 |
| `augment_dry.bat` | 시범 (5개 미리보기) |
| `augment_full.bat` | v1 전체 (위험 — 마지막 저장) |
| `augment_v2.bat` | **v2 전체 (안전)** ⭐ |
| `analyze_augment.bat` | augment 결과 자동 분석 |
| `test_worker.bat` | Worker 응답 진단 |

## 📌 다음 세션 즉시 진행 가능한 액션

### 1순위: augment_v2 마저 진행
```
augment_v2.bat 더블클릭
```
- 이전 세션에서 끊겼으면 자동 이어감 (`.augment_progress.json` 확인)
- 25개마다 저장이라 안전

### 2순위: 결과 분석 + push
```
analyze_augment.bat   # 결과 카테고리별 비교
auto_push.bat          # GitHub 반영
```

### 3순위: 카톡 친구 피드백 다시 받기
- URL: https://ilsanintel0602-collab.github.io/yeoguiseon/app.html?v=24
- 친구들이 같은 사진 다시 찍어서 정확도 비교

## 🐛 알려진 이슈

### 1. augment v1 (`augment_full.bat`) — 사용 금지
- 마지막에 한 번 저장이라 중간 끊김 = 손실
- **v2(`augment_v2.bat`)만 사용**

### 2. Cowork 채팅창 + 슬래시 코드
- 코드에 `/`, `//`, `/*` 등이 들어가면 Cowork이 "알 수 없는 스킬"로 막음
- 채팅으로 코드 공유 시 백틱(\`\`\`) 코드블럭 권장
- **Cloudflare 편집기 직접 붙여넣기는 영향 X** (외부 사이트)

### 3. Cloudflare Worker 수동 배포 필요
- `cloudflare_worker.js` 변경 시 → Cloudflare 대시보드에서 복사 붙여넣기 + Deploy
- `auto_push.bat`은 GitHub만 갱신 (Cloudflare 별개)
- 옵션: 향후 Wrangler CLI 자동 배포 가능 (현재는 보류)

## 🔑 5개 동기화 지점 (오류 방지)

| # | 위치 | 동기화 |
|---|---|---|
| 1 | PC 파일 | 원본 |
| 2 | GitHub | auto_push.bat 자동 |
| 3 | GitHub Pages | 자동 빌드 (1~2분) |
| 4 | 모바일 PWA | Service Worker VERSION (수동 새로고침 가능) |
| 5 | Cloudflare Worker | **수동** (대시보드 복사+Deploy) |

## 💡 사용자 약속

1. **버전은 큰 기능 추가만** (작은 데이터 보강은 버전 안 올림)
2. **에러를 두 번 만나지 않게** (시스템화)
3. **API 키는 평생 사용자가 안 봄** (Worker 안에만)
4. **데이터 가치 우선** (수동 alias 추가 X, 자동 시스템 O)

## 🎯 단기 로드맵

- [ ] augment_v2 완료 (765 items × ~10 alias = ~7,000 추가)
- [ ] 카톡 친구 피드백 → analyze_feedback.py로 약점 자동 보강
- [ ] Phase 4: data.go.kr 폐의약품 수거함 크롤링 (publicDataPk 확정 필요)
- [ ] 벤치마크 데이터셋 (TrashNet) 객관 점수 측정

## 📝 메모리 갱신 사항

- `project_v4_status.md` — v5.24 + Worker v1.7.2 반영
- `feedback_workmode.md` — augment_v2 안전 모드 추가
- 신규: `project_v5_24_handoff.md` — 이 문서 요약 링크
