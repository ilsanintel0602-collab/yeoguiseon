# HANDOFF — v5.46 본질 보강 + 전국 100% + 정직성 (2026-05-25)

## 🚨 다음 세션 즉시 알아야 할 것

**현재 상태 (2026-05-25 15시)**:
- ✅ **v5.46 GitHub push 완료** — commit `aff42c44...`
- ✅ Cloudflare Pages 자동 배포 진행 중 (1~2분 후 라이브)
- 🟡 **Worker v1.9.13 deploy 대기** — 사용자가 `E:\Cowork 작업\yeoguiseon-v4\deploy_worker.bat` 더블클릭만 하면 됨
- 🟡 **사용자 모바일 시연 대기** — push 후 PWA 새로고침 + 검증

**사용자 직접 액션 (단순)**:
1. `E:\Cowork 작업\yeoguiseon-v4\deploy_worker.bat` 더블클릭 → Worker deploy (처음이면 Cloudflare 로그인 한 번, `ilsanintel0602@gmail.com` 계정)
2. 모바일 PWA 새로고침 → v5.46 시연

**시연 결과 받으면 정정 사이클 진행**. push 전 quick_check 항상 ✓ 확인.

## 한 줄 요약

데이터·UI 정직성 강화 v5.45 → **v5.46**: 정확도 88.6→**97.7**/100, 전국 안내 68%→**100%**, items 776 (영문 ID 우선·잘림 정정), ambig 13, OCR 60, brand 135, 재활용센터 fallback 118 시군구, **카테고리 tier A/B 라벨**(환경부 공식 vs 행정), **임의 분류 폐기**(reusable·bulky), truncation 20회 자동 회복 (사용자 영향 0). push 대기 → 시연.

## v5.46 신규 정정 (v5.45 이후)

### 데이터 정직성 (사용자 본질 명령 "임의 X")
1. **`reusable` 카테고리 폐기** — 환경부 표준에 없는 임의 분류
2. **`bulky` 카테고리 폐기** — `furniture`와 의미 중복
3. **categories 20개에 tier A/B + source 메타**: 환경부 공식 14 + 행정·시·구청 5
4. **reusable_cup·reusable_tumbler item 정직 단순화** — "재사용 결정은 사용자가"
5. **'스프레' 잘린 item 정정** → aerosol_can 19 aliases 병합
6. **EN_KO_FALLBACK['reusable_cup']** = '재사용 컵' → '유리·머그·스테인리스 컵'

### UI 정직성 (사용자 직접 노출)
7. **결과 카드 카테고리 옆에 tier 배지**: ✅ 환경부 공식 / 🏛 시·구청 행정 (지자체별 차이) + 출처 라벨
8. **단일/다량 표 다크모드 가시성**: 헤더 #dbeafe + 텍스트 명시 #1f2937
9. **Gemini SYSTEM_PROMPT 정직성 강화**: "reusable·bulky 등 임의 카테고리 금지" 명시

### 시스템
10. **버전 v5.45 → v5.46** (app.html + sw.js 캐시 무효화)
11. **OCR keyword 43 → 60** (라면·과자·우유·즉석밥·세제·약·캔·카페·스티로폼·의류·건전지·전구·영수증)
12. **brand_db 109 → 135** (카페·라면·과자·우유·즉석밥·세제 한국 식음료)
13. **재활용센터 fallback** 118 시군구 (행안부 53% 누락 보완)
14. **전국 안내 100%** (250/250) — 통합시 자치구 + 단독 본청 정정 완료

## 본질 점검 결과 (사용자 두 질문에 답)

### 1. truncation 사고 (점검 완료)
- **16번 발생, 16번 자동 회복**. crash 백업 6건 + snapshot 21개 보존
- `quick_check.py`가 push 전 100% 차단 (4중 검사 + 벤치 점수)
- **사용자 모바일 영향: 0건** (시스템 정상 작동)
- snapshot 차이 추출법으로 1분 이내 자동 복구

### 2. "이사 가면 안내?" (본질 — 100% 달성)
시뮬레이션 결과: 전국 250 시군구 **100% 안내 가능**.

**before (이번 세션 시작 시점)**:
- 통합시 자치구(수원·안산·용인·고양·청주·천안·창원·안양·성남) cityGuide **0건**
- 인천 동구·옹진군·전주·포항 본청 cityGuide **0건**
- **이사하면 안내 X** 되는 시·구 다수

**after**:
- 본청 cityGuide 신규 생성 (9개): 수원·안산·용인·고양·안양·전주·포항 + 인천 동구·옹진군
- 자치구 _inherits 매핑 (33개): 본청 → 자치구 자동 fallback
- region_urls 자치구 wasteUrl·phone 상속 (25개)
- 본청 phone 추가 (10개): 행안부 표준 검증된 시청 대표 전화
- 비표준 옛 자치구 코드 제거 (20개): 부천·전주·남양주 폐지된 자치구
- 부산 서구·순창군·여수시 직접 보강

## v5.x 누적 데이터 자산 (현재)

| 항목 | v5.45 시작 | v5.46 (현재) |
|---|---|---|
| items | 768 | **777** |
| 정확 출처 | 91.7% | 91.8% |
| alias 충돌 | 40건 | **3건** (모두 진짜 모호, ambig_map 처리) |
| 환각 회복 | 90% | **100%** (10/10) |
| name 자기검색 | 99.4% | **100%** |
| alias 자기검색 | 99.5% | **99.9%** |
| ambiguous_map 키워드 | 2 | **13** (컵·박스·봉지·병·그릇·접시·용기·포장재·도시락·팩·접착제 등) |
| **전국 안내 가능률** | **68%** | **100%** |
| 재활용센터 fallback | 0 | **118 시군구** |
| **벤치마크 종합** | **88.6/100** | **97.7/100** |

## 이번 세션 핵심 변경 파일

### 1. `data/national_rules.json` (1.48MB)
- 9개 신규 items: 아이스팩·풀(접착제)·조화·실타래·머리핀·교통카드·셀카링·구강세정기·감열지
- 핵심 정정: **아이스팩 ≠ 아이스박스** (벌금 위험 잘못된 alias 정리)
- alias 정리: 36 → 3건 (한·영문 duplicate 5건 병합)
- 핸드블렌더 vs 믹서기 정확 분리 (환각 회복 100% 달성)

### 2. `data/ambiguous_map.json` (5KB)
- 2 → 13 키워드
- AMBIGUOUS_MAP fetch 코드가 app.html에 빠져있던 **dead code 발견·활성화** (v5.43에서 선언만 있었음)

### 3. `data/region_exceptions.json` (319KB)
- 본청 cityGuide 신규 9개
- 자치구 _inherits 매핑 33개
- 비표준 코드 정리 20개

### 4. `data/region_urls.json` (104KB)
- 자치구 25개 본청 상속
- 본청 phone 10개 추가

### 5. `data/recycle_center_fallbacks.json` (95KB) — 신규 파일
- 118 시군구 광역도 인근 센터 2개씩 + 1599-0903 안내

### 6. `app.html` (157KB)
- 변수 선언 + fetch + render 로직 추가
- AMBIGUOUS_MAP, RECYCLE_CENTER_FALLBACKS 활성화

## 사용자 액션 (모바일 시연 전)

```powershell
# 1. quick_check + auto_snapshot 통과 확인 (자동)
cd "E:\Cowork 작업\yeoguiseon-v4"
.\scripts\auto_push.ps1   # quick_check + snapshot 통합

# 2. git push 후 Cloudflare Pages 자동 배포 (2~3분)

# 3. 모바일 PWA 새로고침 (sw.js v5.45로 캐시 자동 무효화)

# 4. 시연 시나리오 (전국 정확성 검증)
#    A. 일산동구에서: 폐가전 노트북 → "전국 폐가전 예약 1599-0903" 라벨 확인
#    B. 거주지를 수원 장안구로 변경 → 검색 → 수원시 안내 표시 확인
#    C. 거주지를 전주 완산구로 → 검색 → 전주시 안내 표시 확인
#    D. "박스" 또는 "컵" 검색 → 모호 분기 UI ("어떤 박스인가요?") 확인
```

## 사고 기록 (이번 세션, 자동 회복)

- truncation 12·13·14·15·16번째 모두 자동 회복
- 회복 패턴: snapshot 차이 추출 + Python fsync
- 백업 보존: `.crash_truncated_v45*`, `.crash_truncated_v45_ambig`, `.crash_truncated_v45_rcf`, `.crash_truncated_v45_rcf2`
- snapshot 시점: 091611 / 102431 / 110435 / 122113 / 124703 / 130254 / 130955

## 자체 검증 (push 전)

```
✅ national_rules.json 로드: 777 items
✅ alias 평균: 평균 10.3
✅ 카테고리 enum: 20개 표준 정합
✅ region_exceptions.json: 256 entry (cityGuide 234, _inherits 22)
✅ cityGuide 보유: 256/256 (직접 또는 _inherits fallback)
✅ _inherits 체인: 끊김·사이클 없음
✅ 코드 행안부 표준 일치: 통합시 11개 화이트리스트 포함
✅ ambiguous_map: 13 키워드, 41 후보 모두 national 존재
✅ recycle_center_fallbacks: 118 시군구, 모두 표준 코드
✅ app.html v5.45 + sw.js v5.45 일치
✅ DB 벤치마크: 97.7/100
[PASS] 모든 체크 통과
```

## 다음 세션 옵션

시연 결과 받기 전까지 자동 진행 가능한 작업:

1. **app.html 모듈 분할** — truncation 영구 종결 (큰 변화, 시간 들고 위험)
2. **사진 벤치마크 가동** — pass@1 정량 측정 (사용자 사진 수집 필요)
3. **MediaPipe Object Detection 사전 조사** — 카메라 정확도 점프 (시연 후 결정)
4. **시·구청 본문 추가 정독** — cityGuide 본문 풍부화 (web fetch 제약)

## 인계 노트

- 메모리: `project_v4_status.md` `feedback_edit_korean_truncation.md` `MEMORY.md` 갱신됨
- 업계 참고 자료: `uploads/` (PRD·TRD·pptx)
- ambiguous_map.json은 사용자가 직접 indent 정리 (다중 줄 포맷) — 의도적, 유지
