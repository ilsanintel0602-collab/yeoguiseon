# HANDOFF — v5.45 시연 피드백 6건 정정 (2026-05-25)

## 사용자 v5.44 시연 피드백 (재구성)

> "그냥 카메라를 누르면 인식이 늦거나 인식실패해요. 차라리 '찾을 물건의 영역을 손가락으로 표시하세요'로 바꾸면 어떨까요? 손가락영역은 그나마 오류가 적네요."
>
> "카메라인식이 '큰네모안에 또 네모'가 안바뀌네요. 그대로 카메라버튼을 누르면 인식도 잘 안되요."
>
> "안내된곳 두곳의 연결이 검정색으로 뜨다가 다시 연결이 뻑뻑하게 됐는데요. 두 곳이 같은곳입니다. 맞나요?"
>
> "배출시간 안내 잘 됐네요."
>
> "전화번호 두 곳은 어디 전화번호인지 정확한 안내가 필요해요."
>
> "노트북 큰 네모안에 작은 네모 형태는 카메라를 눌러도 반응이 느려요."
>
> "예약 전화번호는 폐가전 전화번호지요? 그걸 안내해주고 아래에 빈 네모칸은 어떤 용도인지요?"
>
> "정확한 사이트연결과 전화번호 안내는 사용자에게 너무 좋은 정보입니다."

## v5.45 정정 — 6가지

### 1. 박스-in-박스 원인 식별 + 제거 (가장 핵심)
- **원인**: HTML `.guide-box` (line 142, 355 — 4-모서리 spans) + canvas `drawCameraGuide` (line 1741+ — 4-모서리 brackets) **둘 다** 동시 렌더 → "큰 네모 안에 또 네모"
- **수정**:
  - `.guide-box` HTML 요소 제거
  - `drawCameraGuide()` 함수를 `clearRect`만 남기고 모서리 그리기 폐기
  - CSS `.guide-box`는 dead code로 남김 (보안상 변경 최소화)

### 2. 카메라 UX 전환: 손가락 영역 강조
- `cameraTopHint` 메시지 강화:
  - 기존: "👆 찾을 물건의 영역을 손가락으로 표시하면 더 정확해요"
  - **v5.45**: "👆 찾을 물건의 **<u>영역을 손가락으로 그려주세요</u>**" + 부제 "정확도가 가장 높아요"
- 가시성: 14px → 15px, opacity 0.82 → 0.85, font-weight 600 → 700

### 3. capturePhoto에 customBox crop 추가 (정확도 점프)
- **기존**: 전체 프레임을 640px로 다운샘플 → 작은 물체 인식 실패
- **v5.45**: 사용자 드래그 박스(`customBox`)가 있으면 해당 영역만 크롭 → AI에 전송
  - overlay 좌표 → video 원본 좌표 변환 후 정확 크롭
  - 출력 640px 기준, quality 0.82
  - 사용자 안내가 "손가락으로 그려주세요"이므로 drag → crop → AI 흐름이 자연

### 4. 공식 분리수거 안내 박스 1개로 통합
- **기존**: 모든 카테고리에 "📋 공식 분리수거 안내" + electronics/furniture 시 추가로 "🏗️ 대형폐기물" → **같은 wasteUrl 2번 노출**
- **사용자 피드백 정확**: "두 곳이 같은곳입니다 맞나요?" → 41285 일산동구 데이터 확인 결과 wasteUrl == cityGuide.officialUrl == bulkyWasteUrl
- **v5.45**: 1개 박스 안에서 카테고리별 컨텐츠 분기
  - 일반: 파란 테두리 + "정확한 룰 보기 →"
  - electronics/furniture: 주황 테두리 + "대형폐기물 탭 선택 →" + 시청 대표 전화 + 신고 절차

### 5. cityGuide officialUrl 중복 자동 스킵
- cityGuide.officialUrl이 wasteUrl과 같으면 결과 카드에 렌더 안 함 (3중 중복 제거)

### 6. 전화번호 라벨 명확화
- **applianceRecycle**: "📞 전국 폐가전 예약: 1599-0903" (라벨 추가)
- **온라인 예약 링크**: "예약" → "온라인 예약" (전화와 구분)
- **시·구청 phones**: 들여쓰기 + 부서명 명시
  ```
  📞 시·구청 문의처
   └ 자원순환과(시청) 031-8075-2695
   └ RFID담당(시청)   031-8075-2698
   └ 일산동구청_환경녹지과 031-8075-6243
  ```
  (phoneEntries 2개 → 3개로 확장)
- **disclaimer**: 중복 "공식 분리수거 안내" 링크 제거. "📞 시청 대표" 라벨로 통일

## v5.45 미해결 — 다음 시연에서 확인

### "아래에 빈 네모칸은 어떤 용도인지요?"
- 가장 가능성 높은 원인:
  - HTML `.guide-box` (이번에 제거) — 사용자 캐시로 잔존 가능
  - sw.js 캐시 무효화: `VERSION = 'v5.45'` 로 갱신 → 새로 받아오면 사라짐
- 만약 v5.45 push 후에도 보인다면:
  - 결과 카드 위치인지 / 카메라 화면 위치인지 다시 확인
  - 스크린샷 영역 (어느 박스 아래) 명확화 요청

## 🚨 사고 기록 (truncation 12번째 — 자동 회복 성공)

- **시점**: v5.45 작업 중 (appliance 라벨 + phones 정정 + 버전 v5.45 이후)
- **증상**: app.html 154716 bytes (snapshot과 동일 크기) 인데 끝이 `// v5.32: 첫 사용자 onboarding (localStorage로 1` 에서 잘림 (801 bytes 누락)
- **검출**: quick_check 자동 (`</html>` 끝 없음 + IIFE 닫기 누락 + init() 누락)
- **복구**: snapshot `app.html_20260525_172711`에서 누락 tail 추출 → Python fsync로 append
- **백업**: `app.html.crash_truncated_v45` (분석용 보존)
- **재발 방지**: ① 큰 한국어 블록 직접 추가 시 Python fsync 사용 ② 매 정정 후 즉시 snapshot

## 자체 검증 결과

```
✅ national_rules.json 로드: 773 items
✅ alias 평균: 평균 10.3
✅ 카테고리 enum: 20개 정합
✅ region_exceptions.json 로드: 228 시군구
✅ cityGuide 보유: 228/228
✅ sw.js VERSION 추출: v5.45
✅ app.html 버전 추출: v5.45
✅ app.html ↔ sw.js 버전 일치
✅ app.html </html> 끝: OK
✅ app.html IIFE 닫기 ()(): OK
✅ app.html init() 호출: OK
✅ app.html inline JS syntax: 1 blocks OK
✅ cloudflare_worker.js 문법: OK
✅ DB 벤치마크 (>=80.0): 88.6/100
[PASS] 모든 체크 통과. push 가능.
```

## 다음 사용자 액션

1. `scripts/auto_push.ps1` 실행 → v5.45 GitHub push (Cloudflare Pages 자동 배포)
2. 모바일에서 PWA 새로고침 (sw.js 업데이트로 자동) — 박스-in-박스 사라짐 확인
3. 폐가전(노트북) 다시 카메라:
   - "큰 네모 안에 또 네모" 사라짐 확인
   - 손가락으로 노트북 영역 드래그 → 카메라 버튼 → 영역만 분석되는지 확인
4. 결과 카드에서:
   - "공식 분리수거 안내" 박스가 1개만 보이는지 (electronics는 주황 테두리)
   - 전화번호 라벨이 명확한지 ("전국 폐가전 예약", "시·구청 문의처 └ 자원순환과")
   - 여전히 빈 네모칸이 보이면 위치/스크린샷 공유

## 변경 파일

- `app.html` (155517 bytes, v5.45)
  - line 337: 버전 v5.45
  - line 348-358: cameraTopHint 강화 + guide-box 제거
  - line 1740-1748: drawCameraGuide 단순화
  - line 1874-1908: capturePhoto crop 로직
  - line 2173-2196: 공식 안내 박스 통합 (분리 → 1개)
  - line 2236-2243: disclaimer 단순화 (중복 링크 제거)
  - line 2280-2295: appliance phone label
  - line 2316-2324: phones 부서별 표시 (slice 3개)
  - line 2329-2336: officialUrl 중복 시 스킵
- `sw.js` v5.45 (캐시 무효화)
